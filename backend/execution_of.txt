# Entorno virtual
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# Ejecucion servidor uvicorn con fastAPI
uvicorn app.main:app --reload

# Ejecución docker (tras copiar el repositorio)
docker compose up --build
docker compose up, docker stop container_id, docker start container_id
docker images, docker ps -a (contenedores),
docker compose exec container_name bash

# Base de datos
psql -U packetscope -d packetscope